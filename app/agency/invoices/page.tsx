'use client'
import{useEffect,useState}from'react'
import{supabase}from'@/lib/supabase'
import{useRouter}from'next/navigation'
import AgencySidebar from'@/components/AgencySidebar'
export default function InvoicesPage(){
const router=useRouter()
const[invoices,setInvoices]=useState([])
const[loading,setLoading]=useState(true)
const[filter,setFilter]=useState('')
useEffect(()=>{async function load(){const{data:{session}}=await supabase.auth.getSession();if(!session){router.push('/login');return};const{data}=await supabase.from('invoices').select('*,bookings(*,venues(name),artists(stage_name))').order('created_at',{ascending:false});if(data)setInvoices(data);setLoading(false)};load()},[]);
const totals={paid:invoices.filter(i=>i.status==='paid').reduce((s,i)=>s+i.amount,0),pending:invoices.filter(i=>i.status==='pending').reduce((s,i)=>s+i.amount,0),overdue:invoices.filter(i=>i.status==='overdue').reduce((s,i)=>s+i.amount,0)}
async function updateStatus(id,status){const{error}=await supabase.from('invoices').update({status,paid_at:status==='paid'?new Date().toISOString():null}).eq('id',id);if(!error)setInvoices(prev=>prev.map(i=>i.id===id?{...i,status}:i))}
const filtered=invoices.filter(i=>!filter||i.status===filter)
const STATUS={paid:{label:'Paid',color:'bg-green-900/30 text-green-400'},pending:{label:'Outstanding',color:'bg-yellow-900/30 text-yellow-500'},overdue:{label:'Overdue',color:'bg-red-900/30 text-red-400'}}
if(loading)return(<div className="min-h-screen bg-[#0E1117] flex items-center justify-center"><div className="text-[#C8A24A] text-4xl font-bold animate-pulse">VE</div></div>)
return(<div className="min-h-screen bg-[#0E1117] flex"><AgencySidebar/><div className="flex-1 flex flex-col"><div className="bg-[#151A22] border-b border-[#263044] px-8 h-14 flex items-center justify-between"><div className="text-white font-semibold">Invoices</div></div><div className="p-8"><div className="grid grid-cols-3 gap-4 mb-8">{[{label:'Paid',value:totals.paid,color:'text-green-400',status:'paid'},{label:'Outstanding',value:totals.pending,color:'text-yellow-500',status:'pending'},{label:'Overdue',value:totals.overdue,color:'text-red-400',status:'overdue'}].map(({label,value,color,status})=>(<div key={label} onClick={()=>setFilter(filter===status?''  :status)} className={'bg-[#151A22] border rounded-xl p-5 cursor-pointer transition-all '+(filter===status?'border-[#C8A24A]':'border-[#263044]')}><div className="text-[#6A7A8A] text-xs uppercase tracking-widest mb-2">{label}</div><div className={'text-3xl font-bold '+color}>£{value.toLocaleString()}</div><div className="text-[#4E5A6A] text-xs mt-1">{invoices.filter(i=>i.status===status).length} invoices</div></div>))}</div><div className="flex gap-2 mb-6">{[{value:'',label:'All'},{value:'paid',label:'Paid'},{value:'pending',label:'Outstanding'},{value:'overdue',label:'Overdue'}].map(({value,label})=>(<button key={value} onClick={()=>setFilter(value)} className={'px-4 py-1.5 rounded-full text-xs font-semibold uppercase tracking-wider transition-colors '+(filter===value?'bg-[#C8A24A] text-[#0B0D10]':'bg-[#151A22] border border-[#263044] text-[#6A7A8A] hover:text-white')}>{label}</button>))}</div><div className="bg-[#151A22] border border-[#263044] rounded-xl overflow-hidden"><table className="w-full"><thead><tr className="border-b border-[#263044]">{['Venue','Artist','Due date','Amount','Status',''].map(h=>(<th key={h} className="text-left px-4 py-3 text-[#4E5A6A] text-xs uppercase tracking-widest">{h}</th>))}</tr></thead><tbody>{filtered.length===0&&<tr><td colSpan={6} className="text-center py-12 text-[#4E5A6A] text-sm">No invoices found</td></tr>}{filtered.map(inv=>{const s=STATUS[inv.status]||STATUS.pending;return(<tr key={inv.id} className="border-b border-[#263044] hover:bg-[#1C2330] transition-colors"><td className="px-4 py-3 text-white text-sm font-medium">{inv.bookings?.venues?.name||'—'}</td><td className="px-4 py-3 text-[#6A7A8A] text-sm">{inv.bookings?.artists?.stage_name||'—'}</td><td className="px-4 py-3 text-[#6A7A8A] text-xs">{inv.due_date?new Date(inv.due_date).toLocaleDateString('en-GB',{day:'numeric',month:'short'}):'—'}</td><td className="px-4 py-3 text-[#C8A24A] font-bold">£{inv.amount.toLocaleString()}</td><td className="px-4 py-3"><span className={'text-xs font-semibold px-2 py-1 rounded-full '+s.color}>{s.label}</span></td><td className="px-4 py-3"><div className="flex gap-2">{inv.status!=='paid'&&<button onClick={()=>updateStatus(inv.id,'paid')} className="text-xs text-green-400 hover:text-green-300">Mark paid</button>}{inv.status==='pending'&&<button onClick={()=>updateStatus(inv.id,'overdue')} className="text-xs text-red-400 hover:text-red-300">Mark overdue</button>}</div></td></tr>)})}</tbody></table></div></div></div></div>)}